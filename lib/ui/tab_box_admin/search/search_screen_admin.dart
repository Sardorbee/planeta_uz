import 'package:flutter/material.dart';

class SearchScreenAdmin extends StatelessWidget {
  const SearchScreenAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Search Screen'),
      ),
    );
  }
}
